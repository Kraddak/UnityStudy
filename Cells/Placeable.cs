using System.Collections;
using System.Collections.Generic;
using UnityEngine;


namespace k4k {
    public interface Placeable {

        //bool IsWalkable(ref Placeable placedObject);
        bool IsWalkable();

    }
}